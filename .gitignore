node_modules/
.vercel/

# Regenerable pipeline outputs — large and reproducible via `npm run all`.
# scores.json is the one exception: it's small and is the actual data the
# dashboard renders, so it's tracked for reference/diffing between runs.
data/commits.json
data/prs.json
data/raw/

# Local copy used only to serve the dashboard during development —
# regenerated by copying data/scores.json before each deploy.
dashboard/scores.json
